pub struct Worker;
pub struct WorkerEvent;
pub enum WorkerEventKind { Started }
pub struct WorkerEventPayload;
pub struct WorkerFailure;
pub enum WorkerFailureKind { Network }
pub struct WorkerPromptTarget;
pub struct WorkerReadySnapshot;
pub struct WorkerRegistry;
pub enum WorkerStatus { Running }
pub struct WorkerTrustResolution;
