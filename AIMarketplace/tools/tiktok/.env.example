# Copy this to .env (gitignored). Paste real keys; NEVER commit .env.

# Preferred — better voice quality
ELEVENLABS_API_KEY=

# Fallback if ELEVENLABS_API_KEY is empty
OPENAI_API_KEY=

# Optional — mix a music bed under the voiceover at about -18 dB
# BG_MUSIC=/path/to/track.mp3
